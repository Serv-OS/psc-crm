===== PHASES =====

## Phase 1 — Schema migration (additive contact_id + relax NOT NULL) + backfill  [risk: low — purely additive + constraint relaxation; no column/table drops, no data loss. The only real-world risk is the live DB diverging from migrations; the explicit DROP NOT NULL statements neutralise that.]
   files: supabase/migrations/066_decouple_company.sql (NEW)
   - Create migration 066 with the full migration_sql above: ADD contact_id to processing_accounts, inv_serials, inv_movements, time_entries, onboardings (all nullable, FK contacts ON DELETE SET NULL); covering indexes on quotes/recurring_invoices contact_id.
   - RELAX NOT NULL on deals.company_id, locations.company_id, tickets.company_id, onboardings.company_id (idempotent — guards against the 005 NOT NULL declarations regardless of live drift).
   - Replace create_onboarding_on_won() so it also resolves and writes the deal's primary contact into onboardings.contact_id (still writes company_id for legacy continuity).
   - Apply migration to the live DB, then run the backfill_sql block (review/enable statement 8 manually — it is LIMIT 0 by default).
   - Do NOT drop any company_id column, FK, or the companies table in this phase.
   verify: Run the migration against a staging copy first. Confirm: SELECT count(*) FROM information_schema.columns WHERE table_name='processing_accounts' AND column_name='contact_id' returns 1; confirm is_nullable='YES' for deals/locations/tickets company_id in information_schema.columns. Re-run the migration once more to prove idempotency (no errors). Spot-check backfill: every invoice/quote row that had a deal/location contact now has contact_id populated.

## Phase 2 — Billing + Payments switch to contact (HIGHEST RISK)  [risk: HIGH — these are the bill-to / money write paths and PostgREST embedded joins. (1) invoices/recurring company_id writes change the owning entity; (2) the `company:companies(name)` embeds in InvoicesPanel ERROR if removed out of step with reads; (3) processing_accounts.contact_id MUST exist (Phase 1) or the AccountModal insert fails on an unknown column; (4) custName/PublicInvoice/PublicQuote labels go blank if the contact fallback is missing.]
   files: src/components/crm/InvoiceBuilder.jsx, src/components/PublicInvoice.jsx, src/components/crm/InvoicesPanel.jsx, src/components/crm/InvoicesCard.jsx, src/lib/invoicePdf.js, src/components/crm/PaymentsPanel.jsx, src/components/crm/ProcessingAccountDrawer.jsx, src/components/crm/ProcessingRatesCard.jsx, src/components/PublicQuote.jsx, supabase/functions/invoice-public/index.ts, supabase/functions/quote-public/index.ts
   - InvoiceBuilder.jsx: remove companies state (line 11) + companies fetch from Promise.all (line 31) + setCompanies (line 40); drop company_id from locations select (line 32); replace the company-based locs filter (line 64) with `const locs = locations;`; remove `company_id: inv.company_id || null` from the save patch (line 75) — contact_id is now the canonical bill-to; remove `const company = companies.find(...)` (line 133) and drop companyName/companyAddress from the billTo object (lines 145-150).
   - InvoicesPanel.jsx: remove companies state (line 23); drop `company:companies(name)` from the invoices join (line 35) and recurring join (line 36) — these PostgREST embeds error once company is gone, so add `contact:contacts(first_name,last_name)` instead; remove companies fetch (line 37); drop company_id from locations select (line 38); remove setCompanies (line 42); change custName (line 57) to `x.location?.name || (x.contact ? [x.contact.first_name,x.contact.last_name].filter(Boolean).join(' ') : '') || x.label || '—'`; drop companies prop from ScheduleModal (lines 162-163, 182); remove company_id from modal form state (line 185); replace locs company filter (line 194) with `const locs = locations;`; remove `company_id: f.company_id || null` from the recurring save row (line 204).
   - InvoicesCard.jsx: remove the companyId prop (line 8); change field default (line 13) to `const field = locationId ? 'location_id' : 'contact_id';` and value (line 14) to `locationId || contactId`; delete the location->company rollup block (lines 26-30); update the doc comment (lines 6-7).
   - invoicePdf.js: drop billTo.companyName/billTo.companyAddress from the billLines array (line 116) so it is `[billTo.contactName, billTo.contactEmail]`.
   - PublicInvoice.jsx: drop `company` from the destructure (line 45); change Bill-to guard (line 77) to `(contact || location)`; use `contact?.name` as the billed-to name (line 80); delete the company address line (82) and the Attn line (83).
   - PublicQuote.jsx: use `data.contact?.name` for prepared-for (line 100); remove the company address line (101).
   - invoice-public/index.ts & quote-public/index.ts: stop fetching/returning `company` (lines 25/27/50 and 32/34/57). Redeploy both edge functions AFTER the front-end no longer reads `company`.
   - PaymentsPanel.jsx: replace companies state with contacts; change the accounts embed (line 51) from `company:companies(name)` to `contact:contacts(first_name,last_name)`; replace the companies fetch (line 53) with `supabase.from('contacts').select('id, first_name, last_name')`; drop company_id from locations select (line 54); repoint setCompanies->setContacts (line 59); change accName fallback (line 75) and the subtitle (line 133) to contact name; pass contacts (not companies) to AccountModal/Drawer (lines 155-158); in AccountModal change the prop, form state f.company_id -> f.contact_id (line 184), the customer-required validation (line 208) to `if (!f.contact_id)`, the insert/update row (line 210) to `contact_id: f.contact_id`, and the Customer <select> (lines 246-249) to map contacts with value=f.contact_id. Remove the location-by-company filter (line 193).
   - ProcessingAccountDrawer.jsx: swap Building2 icon for User (line 3); change companies prop to contacts (line 10); embed `contact:contacts(...)` on reload (line 29); use acc.contact?.name in the header (line 37); change the nav button (line 78) from onNavigate('company', acc.company_id) to onNavigate('contact', acc.contact_id); pass contacts to AccountModal (lines 170-171).
   - ProcessingRatesCard.jsx: rename companyId prop -> contactId (line 8), the effect dep (line 11), and the filter (line 15) from `.eq('company_id', companyId)` to `.eq('contact_id', contactId)`; relabel the 'Company-wide' badge (line 58) to 'All locations'.
   verify: `npm run build` must pass. Manually: raise a NEW invoice from a contact and from a location — confirm it saves and the PDF BILL TO shows the contact name/email; open an existing invoice's public pay page (PublicInvoice) and confirm 'Billed to' shows the homeowner; open the Invoices list and confirm custName shows contact/location not '—'; create a recurring schedule and confirm it saves with contact_id; create a processing account in PaymentsPanel picking a contact and confirm insert succeeds and the drawer header + nav link resolve to the contact; load ProcessingRatesCard for a contact and confirm rows return. Redeploy invoice-public & quote-public, then reload both public pages.

## Phase 3 — CRM record screens + lists/boards (remove cards/columns/selectors/badges + onNavigate('company'))  [risk: MEDIUM overall; HIGH inside DealBoard.jsx, LeadBoard.jsx (deal-insert bill-to write must switch to a primary_contact association) and LocationDetail.jsx (Build Stages re-source — naive deletion silently empties the card). All deal/ticket/lead company_id writes are now safe to drop because Phase 1 relaxed the NOT NULL constraints.]
   files: src/components/crm/DealDetail.jsx, src/components/crm/TicketDetail.jsx, src/components/crm/LeadDetail.jsx, src/components/crm/ContactDetail.jsx, src/components/crm/LocationDetail.jsx, src/components/crm/ProjectDetail.jsx, src/components/crm/TaskDetail.jsx, src/components/crm/FeatureRequestDetail.jsx, src/components/crm/DealBoard.jsx, src/components/crm/LeadBoard.jsx, src/components/crm/TicketList.jsx, src/components/crm/ContactList.jsx, src/components/crm/LocationList.jsx, src/components/crm/ProjectList.jsx, src/components/crm/TaskList.jsx, src/components/crm/OnboardingBoard.jsx, src/components/crm/FeatureRequestList.jsx, src/components/crm/LeadsCard.jsx, src/components/crm/HardwareCard.jsx, src/components/crm/InboxPanel.jsx, src/components/crm/ConversationTimeline.jsx
   - DealDetail.jsx: remove company/companies state (lines 19,21); drop the companies fetch from Promise.all (lines 34,37); drop company_id from locations select (line 38); remove setCompanies (46) and the setCompany line (50); remove `company_id: deal.company_id || null` from the createQuote insert (66-67) — contact_id stays as bill-to; remove `company_id: draft.company_id` from the save patch (78-79); delete the dead companyLocations (131); fix stale comments (206,232).
   - TicketDetail.jsx: remove company/companies state (32,35); change contactContext init to drop companies (39); drop companies from Promise.all (51,54); drop company_id from locations select (57); remove setCompanies (62) and the setCompany block (67-69); in contact-context derivation drop the company half (91-103, change `.in('to_type',['company','location'])` to `.eq('to_type','location')`); remove the createContactFromTicket company association insert (128-133); remove `company_id: draft.company_id` from save patch (146); delete dead companyLocations (187); remove the header company badge (211-216); remove the 'Via Contact' companies branch (390-396) and tighten its guard (387); fix comment (422).
   - LeadDetail.jsx: remove company state (18); remove the company entry from ensureAssociations links (35); remove the company fetch (58); remove `company_id: lead.company_id` from the qualifyLead deal insert (99); drop the company phone fallback (126); fix comment (30-31).
   - ContactDetail.jsx: simplify the '+ Lead' onClick (86-91) to `onCreateLead?.({ contactId })` — remove the associations company lookup.
   - LocationDetail.jsx [HIGH]: remove company state (26); delete the company fetch + setCompany inside the company_id block (56-65); DELETE the deals-by-company fetch and the `deals` state (27,63) — the Deals card already renders via AssociationManager (line 207); RE-SOURCE the onboardings/Build-Stages fetch off the location's linked deal (associations from_type='location' to_type='deal' -> onboardings.deal_id) or onboardings.contact_id, NOT company_id, so Build Stages does not silently empty; remove the header badge-company block (117-121); change the '+ Lead' button (132) to `onCreateLead?.({ locationId })`.
   - ProjectDetail.jsx: remove the 'company' SUBJECT_TYPES entry (14); remove companies state (25); drop companies from Promise.all (40,44); drop company_id from locations/deals/onboardings selects (45-47); remove setCompanies (53); strip company derivation from getLinkedContext (59-78) and loadSubjectRecords (80-100); remove the header company chip (186-191); strip `(co?.name)` suffixes from Location/Deal/Build-Stage option labels (259-263,288-291,304-307); REMOVE the Company <select> in the edit form (265-277); neutralise the building emoji fallbacks (315,359); remove the auto-derived company sub-block in 'Linked To' (366-376) and simplify line 362.
   - TaskDetail.jsx: remove companies state (18); drop companies from Promise.all (30,35); drop company_id from locations/deals selects (36-37); remove setCompanies (43); strip company resolution from getLinkedContext/resolve (108-127); remove the Linked company Card (300-312); simplify the linked-record guard (314-322).
   - FeatureRequestDetail.jsx: remove the Companies tab button (96) and its AssociationManager panel (139).
   - DealBoard.jsx [HIGH]: remove companies/newCompany state (17,27); drop companies fetch (35,37,45); remove the company OR clause in `filtered` (50-57); remove companyName helper (66); delete the setNewCompany auto-derive in handleLocationChange (118); change create validation (125) to require a contact (or location); remove `company_id: newCompany` from the deal insert (128) and instead, after inserting the deal, write an association {from_type:'deal',to_type:'contact',label:'primary_contact'} (mirror LeadBoard line 157); replace the create-form company selector (199-209) with a contacts dropdown bound to newContact; remove the List Company column header (287) and cell (299).
   - LeadBoard.jsx [HIGH]: remove companies/companySearch/selectedCompany state (26,44-45); drop companies fetch (67,69,75); drop the `!companies.length` term (85); remove the prefill.companyId block (88); remove companies from dep arrays (92); remove companyName helper (108) and companyResults memo (113-117); remove `company_id: lead.company_id` from the qualifyLead deal insert (149) — keep the existing primary_contact association write (157); remove resets (224); remove the board-card company badge (357); remove List Company header (394) and cell (408) and fix colSpan 9->8 (422).
   - TicketList.jsx: remove companies/companyId state (28,36); drop companies fetch (54,56,61); remove the company search OR clause (75) and dep (78); remove companyName helper (80); delete the dead handleLocationChange (86-92); remove `company_id: companyId || null` from the ticket insert (100) and the resets (113,168).
   - ContactList.jsx: remove companies state (10); drop companies fetch (23,26,32); remove getCompanyNames helper (73-74); remove `company_id:''` from create-form state (78); remove the contact->company association insert in create() (93-95); delete the Company <select> form field (140-141); remove the 🏢 company chip (167); fix comment (63).
   - LocationList.jsx: remove companies state (17); drop companies fetch (51,53,60); remove the company search OR clause (96) and dep (100); remove companyName helper (102).
   - ProjectList.jsx: remove `company:'Company'` from SUBJECT_LABELS (11); remove companies state (19); drop companies fetch (34,38,45); strip company derivation from getLinkedInfo (70-99); remove the 🏢 company badge in render (186-190).
   - TaskList.jsx: remove companies state (21); drop companies fetch (41,45,52); remove the company branch in resolveSubject (100) and the companyId fields (101-102); remove the company badge push (119-123); simplify the badge render (218-224).
   - OnboardingBoard.jsx [MEDIUM]: remove companies state (7); drop companies fetch (20,22,29); change locationName() to derive the location off o.location_id / deal association only (44-46) — drop the company fallback; remove the company term from search (54,58,64); remove companyName helper (66); update placeholder (99); remove companyName from cardName fallback (115); remove the ['Company',...] row (117).
   - FeatureRequestList.jsx: remove companies state (23); drop companies fetch (38,43,49); remove getLinkedCompanies helper (79-80); change demandCount (82) to count contacts+deals; remove the company chip (147,155).
   - LeadsCard.jsx: cosmetic comment-only edit (line 3) — drop 'company'.
   - HardwareCard.jsx [MEDIUM]: remove the companyId prop (15); remove the `else if (companyId)` branch in load() (24); change the dep array to [locationId] (29); fix comment (13). Optionally add a contactId prop later that resolves the contact's locations via associations.
   - InboxPanel.jsx: remove the findCompanyId helper (254-259); remove `const companyId = await findCompanyId()` (266) and `company_id: companyId` from the tickets insert (273) — contact_id already links the homeowner.
   - ConversationTimeline.jsx: remove the hardcoded `company:''` ctx key (97).
   verify: `npm run build` after each board/detail file group. Manually: create a deal from DealBoard (no company option visible) and confirm it saves + a primary_contact association is written; qualify a lead and confirm the new deal carries the contact, not a company; open LocationDetail and confirm the Build Stages card still lists onboardings (the re-source works) and Deals still render; open every detail screen and confirm no broken company badge/card and no console error from a removed onNavigate('company'); create a ticket from email in InboxPanel and confirm it links to the contact.

## Phase 4 — Nav/routing/search/pages + reporting + inventory + DataPanel + timer/dealHelpers  [risk: HIGH inside StockOutView.jsx, inventoryOps.js, timer.js, dealHelpers.js (all write contact_id which only exists after Phase 1) and CompanyList/CompanyDetail deletion (build breaks if any import/route/onNavigate('company') reference remains). MEDIUM in DataPanel (merge_companies RPC + import branch), ReportingDashboard (Promise.all index renumber), FormBuilder (server-side ingestion still branches on company_* keys).]
   files: src/components/Shell.jsx, src/components/Sidebar.jsx, src/components/GlobalSearch.jsx, src/components/crm/CompanyList.jsx, src/components/crm/CompanyDetail.jsx, src/components/crm/AssociationManager.jsx, src/components/crm/DataPanel.jsx, src/components/crm/ReportingDashboard.jsx, src/components/crm/ModulesPanel.jsx, src/components/crm/FormBuilder.jsx, src/components/inventory/StockOutView.jsx, src/components/inventory/StockView.jsx, src/components/inventory/InvReportsView.jsx, src/lib/inventoryOps.js, src/lib/timer.js, src/lib/dealHelpers.js
   - Shell.jsx: remove the CompanyList/CompanyDetail imports (26-27); delete the `if (type==='company')` nav case (113-114) and promote the next branch to the leading if; delete the 'companies' and 'company_detail' renderMain switch cases (153-157); update the startLead comment (78-79). Default view 'contacts' stays.
   - Sidebar.jsx: remove the `company_detail:'companies'` key from ACTIVE_MAP (45). KEEP the Building2 icon import (5) — still used by Departments.
   - GlobalSearch.jsx: remove the 'company' GROUPS entry (5); remove the companies query from runSearch Promise.all (39-40); remove the company setResults mapping (49).
   - CompanyList.jsx: DELETE the whole file (only importer is Shell, removed above).
   - CompanyDetail.jsx: DELETE the whole file. Its billing/processing/hardware cards (ProcessingRatesCard/HardwareCard/InvoicesCard) are RE-HOMED onto ContactDetail in Phase 2/3 (keyed by contactId) — verify ContactDetail mounts them before deleting, or those features become unreachable.
   - AssociationManager.jsx: remove the `targetType==='company'?'companies'` arm in the load() table chooser (34-36) and the 'Companies' typeLabel arm (92); optionally change getTargetSub final fallback to `return ''` (75). Grep the repo for any remaining targetType="company" caller first.
   - DataPanel.jsx [MEDIUM]: remove the entire `companies` import TARGET block (6-15); remove the contacts 'company' link field (24); change ImportTab default target to 'contacts' (56); remove the company preload (88-89) and the `if (target==='companies')` import branch (99-109); remove the contact->company link block (121-133); remove the companies <option> (152); simplify skip-dupes label (181); remove companies-only disabled/hint (184,188); change DuplicatesTab default to 'contacts' (209); strip company branches in detect()/mergeGroup (219-243,250); remove the companies dupe <option> (265).
   - ReportingDashboard.jsx [MEDIUM]: remove companies state (23); remove the companies fetch from the Promise.all (44) and re-index results[] (safest: replace the slot with `Promise.resolve({data:[]})` to avoid renumbering); remove setCompanies (58); drop the 'Company' column from Sales (227-231), Build-Stages (338-342) and Support (360-364) CSV exports; remove the Companies MetricCard (409) and re-balance the grid to cols-3; remove the 'Export companies CSV' button (438-442).
   - ModulesPanel.jsx: remove companies state (15); drop companies fetch (45,48) and company_id from locations select (47); remove setCompanies (53); remove companyName helper (58) and the company subline in the matrix cell (184).
   - FormBuilder.jsx [MEDIUM]: remove the Company mapping group from MAPS_COMMON (13-16). NOTE: the server-side form-ingestion edge fn (forms-public) still branches on company_name/company_domain/company_city — update it to ignore those keys so they no-op cleanly.
   - StockOutView.jsx [HIGH]: rename companies->contacts state (10) and companyId->contactId (14); fetch contacts `select('id, first_name, last_name')` (27); REMOVE the company-scoped location plumbing (28,38,119-123) — homeowners have no sub-sites, so drop the Location(site) select and location_id writes; change the staged embed (30) to drop company:companies(name) and add contact:contacts(first_name,last_name); repoint setState (32); rewrite submit() to pass contactId and write contact_id (46,49,50-53); confirmStaged/cancelStaged (71,83) and the grouping key (91) to contact_id; rebuild the selector UI (114-127) as a contacts dropdown labelled 'Customer (homeowner)'; staged-card target/badge (161,166) to contact name.
   - StockView.jsx [MEDIUM]: change the serials embed (33) to drop company:companies(name) and add contact:contacts(first_name,last_name); replace every r.company?.name in search/recall/CSV/lookup/table (49,87,121,274-276,333-334) with the contact full-name expression; change both onNavigate('company', r.company_id) to onNavigate('contact', r.contact_id) (275,333) — the 'contact' route already exists in Shell.
   - InvReportsView.jsx [MEDIUM]: change the serials embed (20) to drop company:companies(name) and add contact:contacts(first_name,last_name); change the 'Deployed by customer' grouping fallback (48) from r.company?.name to the contact full-name. Valuation sums (35-36) are company-independent — untouched.
   - inventoryOps.js [HIGH]: stockOut() signature drops companyId/locationId, takes contactId (74); validation (76) to contactId; serial update (85-88) writes contact_id not company_id/location_id; ledger write (95-99) writes contact_id; recallToServicing ledger (112-116) carries row.contact_id. Update the comment (73).
   - timer.js [HIGH]: resolvePolymorphic — drop the 'company' branch (13); change location/deal/lead/onboarding/ticket branches to resolve the linked contact (via contact_id column or an associations to_type='contact' lookup) instead of company_id; the contact branch returns {contact_id:id} (26-29); startTimer destructures contact_id and inserts time_entries.contact_id (74-89). KEEP location_id where it is still meaningful.
   - dealHelpers.js [HIGH]: remove `company_id: deal.company_id` from the onboardings insert (21) — set contact_id from the deal's primary_contact association instead; the existing contact-association copy (54-68) already preserves the homeowner; update the doc comment (4-8).
   verify: `npm run build` MUST pass — it will fail loudly if any CompanyList/CompanyDetail/`'companies'`/`'company_detail'` reference survives. Then grep src for: CompanyList, CompanyDetail, 'companies', 'company_detail', onNavigate('company', navigateTo('company', targetType="company", merge_companies — expect zero hits except KEPT items. Manually: GlobalSearch returns contacts/locations/deals (no company group); Sidebar highlights correctly; stock-out a serial to a contact and confirm it saves with contact_id and appears in StockView grouped by the homeowner; deployed-by-customer report groups by contact; close-won a deal and confirm an onboarding is created with contact_id (trigger + JS); start a timer on a deal/ticket and confirm time_entries.contact_id is set; run a DataPanel contacts import.

===== KEEP =====
 - companies TABLE + companies.id and every company_id COLUMN/FK across deals, locations, tickets, onboardings, invoices, quotes, recurring_invoices, processing_accounts, inv_serials, inv_movements, time_entries, leads — kept as legacy/transitional (non-destructive). Dropping them is a separate later phase once all reads/writes are gone and verified in prod.
 - supabase/migrations/058_onboarding_on_won.sql trigger create_onboarding_on_won — KEPT and updated (not removed). It still references NEW.company_id; deals retains company_id so the trigger keeps working while also now writing contact_id.
 - deals.deal_id-style pipeline links: quotes.deal_id and deals associations — KEEP (pipeline link, NOT a company/bill-to concept; the prompt explicitly warns not to touch deal_id).
 - src/lib/leadStages.js — KEEP intact. Zero company references; shared by ContactList/LeadBoard. Modifying it would break lead consumers.
 - Sidebar.jsx Building2 icon import (line 5) — KEEP; still used by the Departments nav item, unrelated to the company entity.
 - AccountPanel.jsx 'Include company logo' toggle (line 322) — KEEP; refers to the SELLER's own brand logo (support_settings.logo_url), not a CRM company record. False positive on the word 'company'.
 - SettingsPanel.jsx Quote-branding card 'company details' (lines 380,387,434) — KEEP; this is the user's OWN business identity shown as the seller on quotes, not a CRM company. False positive.
 - InvReportsView.jsx valuation sums stockValue/deployedValue and fmtGBP — KEEP unchanged; company-independent (sum r.cost). Removal of company only affects the grouping label, not the money.
 - locations.company_id, deals.company_id etc. remaining in the DB after the contact switch — KEEP populated where they already are; do not null them out (the backfill only ADDS contact_id, it never clears company_id).
 - crm_activities.contact_id, associations table, and the existing invoices/quotes/recurring_invoices contact_id columns — KEEP; these are the established contact-centric convention the refactor builds on.

===== HIGH RISK =====
 - [SCHEMA DRIFT — read first] Migration 005_crm_schema.sql declares deals.company_id, locations.company_id, tickets.company_id as NOT NULL ... ON DELETE CASCADE (and onboardings was NOT NULL until 058). The supplied SCHEMA findings' LIVE introspection reported them as nullable SET NULL — i.e. the live DB has drifted from the migrations. Do NOT trust 'everything is already nullable'. Phase 1 MUST run the explicit `ALTER ... DROP NOT NULL` statements (idempotent) on deals/locations/tickets/onboardings BEFORE any code stops writing company_id, or every deal/lead/ticket insert in Phase 3 throws a NOT NULL violation. VERIFY post-migration: SELECT is_nullable FROM information_schema.columns WHERE column_name='company_id' AND table_name IN ('deals','locations','tickets','onboardings') — all must be 'YES'.
 - [processing_accounts.contact_id must exist before Phase 2] PaymentsPanel AccountModal insert/update writes contact_id; without the Phase-1 column add, PostgREST rejects the unknown column and account creation fails. VERIFY: create a processing account picking a contact and confirm the row saves.
 - [inv_serials.contact_id / inv_movements.contact_id must exist before Phase 4 inventory] StockOutView staged update + inventoryOps.stockOut/recall write contact_id. Without Phase-1 columns these inserts fail and the contact:contacts(...) embeds return null. VERIFY: stock-out a serial to a contact, confirm it persists and shows in StockView grouped by the homeowner.
 - [PostgREST embedded joins error if removed out of step] InvoicesPanel lines 35-36 (`company:companies(name)`), PaymentsPanel line 51, ProcessingAccountDrawer line 29, StockView/InvReportsView serials embeds all break the query the moment the column/FK is gone OR the embed name is wrong. Swap the embed to `contact:contacts(...)` in the SAME edit as removing `company:companies`. VERIFY: each list/drawer loads with no PostgREST 400 in the network tab.
 - [Bill-to ownership flips entity] InvoiceBuilder save patch (line 75), InvoicesCard newInvoice seed (lines 27-30), InvoicesPanel recurring row (line 204), DealBoard deal insert (line 128), LeadBoard qualify insert (line 149) — removing company_id changes which entity OWNS the money record to the contact. Ensure contact_id is ALWAYS populated on save (and for DealBoard, write a primary_contact association after insert). VERIFY: raise an invoice + a recurring schedule + a quote and confirm contact_id is set; check the public pay page bills the homeowner.
 - [LocationDetail Build Stages silently empties] LocationDetail.jsx fetches onboardings WHERE company_id = location.company_id (lines 59-60). The onboardings/Build-Stages card renders from `onboardings` state with NO fallback. Naive deletion empties it. MUST re-source onboardings via the location's linked deal (associations location->deal -> onboardings.deal_id) or onboardings.contact_id. VERIFY: open a location that has build stages and confirm the card still lists them.
 - [CompanyList/CompanyDetail deletion breaks the build if any reference survives] Delete only after Shell imports/cases removed AND ContactDetail hosts the re-homed ProcessingRatesCard/HardwareCard/InvoicesCard. VERIFY: `npm run build` passes; grep src for CompanyList, CompanyDetail, 'companies', 'company_detail', onNavigate('company' — zero hits.
 - [Edge functions still return company] invoice-public (lines 27,50) and quote-public (lines 34,57) fetch and return `company`. Harmless while the column exists, but for full removal update + redeploy them AFTER the front-end stops reading `company`. forms-public ingestion still branches on company_name/company_domain/company_city (FormBuilder mapping) — update it or those form fields silently no-op. VERIFY: reload PublicInvoice/PublicQuote after redeploy; submit a public form and confirm the contact is created.
 - [closed_won onboarding written by BOTH a JS path and a DB trigger] dealHelpers.handleClosedWon (JS) and create_onboarding_on_won (trigger from 058) both insert onboardings and both currently write company_id. The JS dedupes on deal_id. When editing dealHelpers (Phase 4), also keep the updated trigger (Phase 1) consistent — both must write contact_id and neither may require a non-null company_id. VERIFY: drag a deal to Won and confirm exactly ONE onboarding is created with contact_id populated.
 - [ReportingDashboard Promise.all index renumbering] Removing the companies fetch shifts every later results[] index; mis-shifting mis-binds locations/contacts/members. SAFEST: replace the removed slot with `Promise.resolve({data:[]})` rather than renumbering. VERIFY: open the dashboard and confirm Customers/Sales/Build-Stages/Support tabs show correct numbers.
